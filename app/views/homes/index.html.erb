<%= render "shared/flash" %>
<ul class='lists'>
  <% @lists.each do |list| %>
    <li>
      <a href="<%= list_path(list) %>">
        <div><%= list.title %></div>
        <div class="counts">
          <%= count_format(list.notes.active.count, "active") %>
          <%= count_format(list.notes.completed.count, "completed") %>
          <%= count_format(list.notes.archived.count, "archived") %>
        </div>
      </a>
    </li>
  <% end %>
</ul>

<% content_for(:navigation) do %>

  <li><%= link_to "Archived", archived_notes_path %></li>
  <li><%= link_to "Completed", completed_notes_path %></li>
<% end %>