<%= render "shared/errors", object: @note %>
<%= form_for [@list, @note] do |f| %>
  <fieldset>
    <legend>Edit Note</legend>
    <%= f.text_field :title, maxlength: 20 %>
    <%= f.text_area :details, maxlength: 150 %>
    <%= f.submit "Save" %>
  </fieldset>
<% end %>