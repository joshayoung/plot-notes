<%= render "shared/errors", object: @list %>
<%= form_for @list do |f| %>
  <fieldset>
    <legend>Edit List</legend>
    <%= f.text_field :title, maxlength: 20 %>
    <%= f.submit "Save" %>
  </fieldset>
<% end %>